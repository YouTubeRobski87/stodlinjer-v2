# Stödkompassen AI chat (src/pages/api/chat.ts) — server-side only.
# Copy to .env for local dev; set these in the Render dashboard in production.

# Required: OpenAI API key. Read at runtime by the OpenAI SDK.
# (Bytte leverantör 2026-08-12, se commit-meddelandet för bakgrund — samma
# variabelnamn som MittPsykes huvudchatt använder.)
OPENAI_API_KEY=sk-...

# Optional: override the chat model (defaults to gpt-5.4).
# OPENAI_CHAT_MODEL=gpt-5.4

# Google Analytics 4 mätnings-ID (G-...). Inlinas i bygget av Astro (PUBLIC_*),
# så den måste vara satt när `npm run build` körs — även i Render.
# Lämnas tom lokalt om du inte vill skicka dev-trafik till produktionsproperty.
PUBLIC_GA_MEASUREMENT_ID=
